server:
  port: 20020

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Revature-MiniMint/config-server
          clone-on-start: true